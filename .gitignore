__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
data/user_settings.json
data/chatgpt_auth.json
data/app.db
data/app.db-wal
data/app.db-shm
.streamlit/secrets.toml
.DS_Store
*.egg-info/
*.tmp.*

# 회사 PPT 양식·로고(업로드) — 사내 자료, 커밋 금지
assets/*.pptx
assets/*.potx
assets/*.png
assets/*.jpg

# 생성 산출물(교재·PPT·강의계획서 등) — 커밋 금지
_assets/

# 원본 첨삭 자료 — 내부용, 커밋 금지 (반영 결과는 skills/·docs/에 반영)
_guideline/

# 사내 자료·원본·키 포함 — GitHub 업로드 금지
_context/
